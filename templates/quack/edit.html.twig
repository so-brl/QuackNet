{% extends 'base.html.twig' %}

{% block title %}Edit Quack{% endblock %}

 {% block content %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between">
            <h1>Edit Quack</h1>
            <a class="btn btn-primary" href="{{ path('quack_index') }}">back to list</a>
        </div>

    {{ include('quack/_form.html.twig', {'button_label': 'Mêttre à jour'}) }}



    {{ include('quack/_delete_form.html.twig') }}
    </div>
{% endblock %}
