

    {{ form_start(form) }}
<div class="d-flex">
    <div class="mr-3">
        {{ form_row(form.content, {
            label: 'Message'
        }) }}
    </div>

    {{ form_row(form.uploadFileName, {
        label: 'photo'
    })  }}
    {{ form_end(form) }}
    <button class="btn btn-primary mb-5 mt-3">{{ button_label|default('Save') }}</button>
</div>


  